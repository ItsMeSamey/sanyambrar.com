{
  "name": "samey-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open /src/games/wordle/index.html",
    "typecheck": "tsc -p tsconfig.app.json --pretty false && tsc -p tsconfig.node.json --pretty false && tsc -p src/games/keybr/tsconfig.ported.json --pretty false && tsc -p tsconfig.worker.json --pretty false",
    "build": "bun build.ts",
    "build:solid": "bun build.ts solid",
    "build:keybr": "bun build.ts keybr",
    "build:tools": "bun build.ts static",
    "import:keybr-books": "bun scripts/import-keybr-books.mjs",
    "check:keybr-corpus": "bun scripts/check-keybr-corpus.mjs"
  },
  "dependencies": {
    "@kobalte/core": "^0.13.13",
    "idb": "^8.0.3",
    "lucide-solid": "^1.37.0",
    "monaco-editor": "0.56.0",
    "solid-js": "^1.9.15",
    "vditor": "^4.0.0",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vite-plugin-solid": "^2.11.14"
  }
}
