// Generated by build.ts. Do not edit.
const CACHE_PREFIX = 'samey-site-';
const CACHE = CACHE_PREFIX + '1317aced3e987518';
const ROOT = new URL('./', self.registration.scope);
const CORE = ["assets/cnn-BlsjeOsa.wasm","assets/cnn-worker-EAlDjyUS.js","assets/editor.worker-DKXjC3Lf.js","assets/json.worker-izi4BK3w.js","blog/index.html","blog/posts/btop-mutex.html","chain/index.html","index.html","keybr.html","projects/cnn/index.html","projects/oneserial/index.html","projects/reverb/index.html","projects/zhtml/index.html","shared-runtime.js","site-chunks/Blog-BW__zAMH.js","site-chunks/Chain-C5J7sf2C.js","site-chunks/Chain-DCY5CPNL.css","site-chunks/ChainLogo-DR1BBxMF.js","site-chunks/CnnDemo-WJ-DNwCG.js","site-chunks/Home-Cn9yoEsK.js","site-chunks/Project-DqEB6IRS.js","site-chunks/ReverbDemo-Ck4aZOHD.js","site-chunks/SiteChrome-IVctML_b.js","site-chunks/Tools-BCgOlPkQ.js","site-chunks/Tools-Cc6Zw_Yb.css","site-chunks/Work-CNnhgJAZ.js","site-chunks/_.contribution-Dt_JKtMv.js","site-chunks/catalog-DfLaJuYE.js","site-chunks/chain-B_thCScn.js","site-chunks/cpp-C7h46wYY.js","site-chunks/css-DIMkf-bt.js","site-chunks/devicePixelRatio-Cp25_G5D.js","site-chunks/dist-CoN7n9_h.css","site-chunks/dist-D-V03KTb.js","site-chunks/game-settings-C4CUxugX.css","site-chunks/go-C-y9NEjX.js","site-chunks/home-B1ek8DjC.css","site-chunks/html-COrXXd9e.js","site-chunks/java-BEtHBSE6.js","site-chunks/javascript-Ce_uVBvx.js","site-chunks/jsonMode-CMLYkryd.js","site-chunks/jsonMode-CQ_gX_ia.css","site-chunks/markdown-Cimd5fb3.js","site-chunks/monaco-BJ215w34.css","site-chunks/monaco-CS03TgGk.js","site-chunks/monaco.contribution-ZJk6AF14.js","site-chunks/python-Mc9-4hRh.js","site-chunks/register-3lT2DI0E.js","site-chunks/register-BJum9STM.js","site-chunks/register-BS8o1vo_.js","site-chunks/register-Bub9rGiS.js","site-chunks/register-CN7oYYyz.js","site-chunks/register-CUP2zy9n.js","site-chunks/register-CVqW3MzG.js","site-chunks/register-CqAmZdv1.js","site-chunks/register-D5ep0jcl.js","site-chunks/register-DTjIzAYr.js","site-chunks/register-DZP24zwQ.js","site-chunks/register-DbDllAi4.js","site-chunks/register-DzzCiCsS.js","site-chunks/register-oo0gUM6A.js","site-chunks/rust-DdL9SqIa.js","site-chunks/shell-CC2rA5mh.js","site-chunks/site-app-3ufZfw3Y.js","site-chunks/sql-NEE52Syq.js","site-chunks/tools-5tGpwBOH.js","site-chunks/typescript-B1vp4qnr.js","site-chunks/web-PVFkuBVy.js","site-chunks/xml-_HRAfsnN.js","site-chunks/yaml-CDjIOXv6.js","site.css","tools/index.html","wordle.html","work/index.html"];

const relativePath = request => {
  const url = new URL(request.url);
  return url.pathname.startsWith(ROOT.pathname) ? url.pathname.slice(ROOT.pathname.length) : '';
};
const immutableAsset = request => {
  const path = relativePath(request);
  return path.startsWith('site-chunks/') || path.startsWith('assets/') || path.startsWith('keybr-assets/');
};
const cacheKey = request => {
  const url = new URL(request.url);
  url.searchParams.delete('v');
  return url.href;
};
const cacheResponse = async (request, response) => {
  if (response.ok) (await caches.open(CACHE)).put(cacheKey(request), response.clone());
  return response;
};
const staleChunkResponse = request => new Response(
  'location.reload();\nawait new Promise(() => {});\n//# sourceURL=' + request.url,
  { status: 200, headers: { 'Content-Type': 'text/javascript; charset=utf-8', 'Cache-Control': 'no-store' } },
);
const fetchFresh = async request => {
  const response = await fetch(request);
  if ((response.status === 404 || response.status === 410) && immutableAsset(request) && new URL(request.url).pathname.endsWith('.js'))
    return staleChunkResponse(request);
  return cacheResponse(request, response);
};
const networkFirst = async request => {
  try { return await fetchFresh(request); }
  catch (error) {
    const cached = await (await caches.open(CACHE)).match(cacheKey(request));
    if (cached) return cached;
    throw error;
  }
};
const cacheFirstImmutable = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  return fetchFresh(request);
};

self.addEventListener('install', event => {
  event.waitUntil(Promise.all([
    caches.open(CACHE).then(cache => cache.addAll(CORE.map(path => new URL(path, ROOT)))),
    self.skipWaiting(),
  ]));
});

self.addEventListener('activate', event => {
  event.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith(CACHE_PREFIX) && key !== CACHE).map(key => caches.delete(key)))),
    self.clients.claim(),
  ]));
});

const offlineNavigation = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  const url = new URL(request.url);
  if (url.pathname.endsWith('/')) {
    const directoryIndex = await cache.match(new URL('index.html', url));
    if (directoryIndex) return directoryIndex;
  } else if (!url.pathname.split('/').pop()?.includes('.')) {
    const htmlPage = await cache.match(new URL(url.pathname + '.html', url.origin));
    if (htmlPage) return htmlPage;
    const directoryIndex = await cache.match(new URL(url.pathname + '/index.html', url.origin));
    if (directoryIndex) return directoryIndex;
  }
  return cache.match(new URL('index.html', ROOT));
};

self.addEventListener('fetch', event => {
  if (event.request.method !== 'GET' || new URL(event.request.url).origin !== location.origin) return;
  if (event.request.mode === 'navigate') {
    event.respondWith((async () => {
      try {
        const response = await fetch(event.request);
        if (response.ok) {
          (await caches.open(CACHE)).put(event.request, response.clone());
          return response;
        }
        return (await offlineNavigation(event.request)) || response;
      } catch { return offlineNavigation(event.request); }
    })());
    return;
  }
  // Only content-hashed Vite assets are cache-first. Mutable shell/runtime,
  // workers and WASM are network-first so a deployment cannot mix generations.
  event.respondWith(immutableAsset(event.request) ? cacheFirstImmutable(event.request) : networkFirst(event.request));
});
