// Generated by build.ts. Do not edit.
const CACHE_PREFIX = 'samey-site-';
const CACHE = CACHE_PREFIX + '2baa07b92d96130f';
const ROOT = new URL('./', self.registration.scope);
const CORE = ["assets/editor.worker-BbVB3hfz.js","assets/json.worker-2CEmYLij.js","blog/index.html","blog/posts/btop-mutex.html","chain.html","chain/index.html","cnn-worker.js","cnn.wasm","index.html","keybr.html","projects/cnn.html","projects/cnn/index.html","projects/oneserial.html","projects/oneserial/index.html","projects/reverb.html","projects/reverb/index.html","projects/zhtml.html","projects/zhtml/index.html","shared-runtime.js","site-chunks/Blog-D8Nnf5sI.js","site-chunks/Chain-7m_xaCLm.js","site-chunks/ChainLogo-CB5tweN8.js","site-chunks/CnnDemo-Bqa81ifO.js","site-chunks/Home-BIcL5VJK.js","site-chunks/Project-DORRBs0C.js","site-chunks/ReverbDemo-C2txy91R.js","site-chunks/SiteChrome-CrFfeHDr.js","site-chunks/Tools-C2FQIl_x.js","site-chunks/Work-DutbjLZq.js","site-chunks/_.contribution-6IxhvFT6.js","site-chunks/chain-E0mRKd0h.js","site-chunks/cpp-C7h46wYY.js","site-chunks/css-DIMkf-bt.js","site-chunks/dist-Btd9zLMT.js","site-chunks/dist-CoN7n9_h.css","site-chunks/go-C-y9NEjX.js","site-chunks/html-DmX8dx3P.js","site-chunks/java-BEtHBSE6.js","site-chunks/javascript-kVUxlam6.js","site-chunks/jsonMode-CQ_gX_ia.css","site-chunks/jsonMode-DEGHRTVu.js","site-chunks/markdown-Cimd5fb3.js","site-chunks/monaco-B-uMLiLM.js","site-chunks/monaco-BJ215w34.css","site-chunks/monaco.contribution-B3iN2Og_.js","site-chunks/python-BgSZO8HU.js","site-chunks/register-BH-1zy_D.js","site-chunks/register-BLYezv2A.js","site-chunks/register-BeP_l4Ka.js","site-chunks/register-BslV7kPw.js","site-chunks/register-CNwwb1HK.js","site-chunks/register-C_Ey6kkv.js","site-chunks/register-ChEfYaHV.js","site-chunks/register-D2ZOD6DI.js","site-chunks/register-D7LVyHov.js","site-chunks/register-DJjO7pWs.js","site-chunks/register-DN53JPux.js","site-chunks/register-HaPDFvMy.js","site-chunks/register-gBU5hxXW.js","site-chunks/register-mjdZz67V.js","site-chunks/rust-DdL9SqIa.js","site-chunks/shell-CC2rA5mh.js","site-chunks/site-app-BN8qvV5V.js","site-chunks/sql-NEE52Syq.js","site-chunks/tools-q2WeaAqp.js","site-chunks/typescript-jysEa3ZH.js","site-chunks/xml-CqFO9P1K.js","site-chunks/yaml-BnHQ56hr.js","site.css","tools.html","tools/index.html","wordle.html","work.html","work/index.html"];

const relativePath = request => {
  const url = new URL(request.url);
  return url.pathname.startsWith(ROOT.pathname) ? url.pathname.slice(ROOT.pathname.length) : '';
};
const immutableAsset = request => {
  const path = relativePath(request);
  return path.startsWith('site-chunks/') || path.startsWith('assets/');
};
const cacheKey = request => {
  const url = new URL(request.url);
  url.searchParams.delete('v');
  return url.href;
};
const cacheResponse = async (request, response) => {
  if (response.ok) (await caches.open(CACHE)).put(cacheKey(request), response.clone());
  return response;
};
const staleChunkResponse = request => new Response(
  'location.reload();\nawait new Promise(() => {});\n//# sourceURL=' + request.url,
  { status: 200, headers: { 'Content-Type': 'text/javascript; charset=utf-8', 'Cache-Control': 'no-store' } },
);
const fetchFresh = async request => {
  const response = await fetch(request);
  if ((response.status === 404 || response.status === 410) && immutableAsset(request) && new URL(request.url).pathname.endsWith('.js'))
    return staleChunkResponse(request);
  return cacheResponse(request, response);
};
const networkFirst = async request => {
  try { return await fetchFresh(request); }
  catch (error) {
    const cached = await (await caches.open(CACHE)).match(cacheKey(request));
    if (cached) return cached;
    throw error;
  }
};
const cacheFirstImmutable = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  return fetchFresh(request);
};

self.addEventListener('install', event => {
  event.waitUntil(Promise.all([
    caches.open(CACHE).then(cache => cache.addAll(CORE.map(path => new URL(path, ROOT)))),
    self.skipWaiting(),
  ]));
});

self.addEventListener('activate', event => {
  event.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith(CACHE_PREFIX) && key !== CACHE).map(key => caches.delete(key)))),
    self.clients.claim(),
  ]));
});

const offlineNavigation = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  const url = new URL(request.url);
  if (url.pathname.endsWith('/')) {
    const directoryIndex = await cache.match(new URL('index.html', url));
    if (directoryIndex) return directoryIndex;
  } else if (!url.pathname.split('/').pop()?.includes('.')) {
    const htmlPage = await cache.match(new URL(url.pathname + '.html', url.origin));
    if (htmlPage) return htmlPage;
    const directoryIndex = await cache.match(new URL(url.pathname + '/index.html', url.origin));
    if (directoryIndex) return directoryIndex;
  }
  return cache.match(new URL('index.html', ROOT));
};

self.addEventListener('fetch', event => {
  if (event.request.method !== 'GET' || new URL(event.request.url).origin !== location.origin) return;
  if (event.request.mode === 'navigate') {
    event.respondWith((async () => {
      try {
        const response = await fetch(event.request);
        if (response.ok) {
          (await caches.open(CACHE)).put(event.request, response.clone());
          return response;
        }
        return (await offlineNavigation(event.request)) || response;
      } catch { return offlineNavigation(event.request); }
    })());
    return;
  }
  // Only content-hashed Vite assets are cache-first. Mutable shell/runtime,
  // workers and WASM are network-first so a deployment cannot mix generations.
  event.respondWith(immutableAsset(event.request) ? cacheFirstImmutable(event.request) : networkFirst(event.request));
});
