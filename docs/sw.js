// Generated by build.ts. Do not edit.
const CACHE_PREFIX = 'samey-site-';
const CACHE = CACHE_PREFIX + 'f9ced6d46a7ec9d3';
const ROOT = new URL('./', self.registration.scope);
const CORE = ["assets/cnn-BlsjeOsa.wasm","assets/cnn-worker-EAlDjyUS.js","assets/editor.worker-DKXjC3Lf.js","assets/json.worker-izi4BK3w.js","blog/index.html","blog/posts/btop-mutex.html","chain/index.html","index.html","keybr.html","projects/cnn/index.html","projects/oneserial/index.html","projects/reverb/index.html","projects/zhtml/index.html","shared-runtime.js","site-chunks/Blog-BAVhjra2.js","site-chunks/Chain-DWKh1gIT.js","site-chunks/Chain-RAwAYXH1.css","site-chunks/ChainLogo-CgqzlrLt.js","site-chunks/CnnDemo-P7lbeC-p.js","site-chunks/Home-CBpXyd10.js","site-chunks/Project-Xb2U0XzE.js","site-chunks/ReverbDemo-kjbO3o95.js","site-chunks/SiteChrome-Do64HUJ1.js","site-chunks/Tools-98ipk89c.js","site-chunks/Tools-C4n0MwU8.css","site-chunks/Work-C0hggTTK.js","site-chunks/_.contribution-CYReRnM2.js","site-chunks/catalog-DfLaJuYE.js","site-chunks/chain-BbQhBeBj.js","site-chunks/cpp-C7h46wYY.js","site-chunks/css-DIMkf-bt.js","site-chunks/dist-CJ9vpSUF.js","site-chunks/dist-CoN7n9_h.css","site-chunks/game-settings-0Zxoa_jh.css","site-chunks/go-C-y9NEjX.js","site-chunks/home-D0_9myQl.css","site-chunks/html-D46iHh3v.js","site-chunks/java-BEtHBSE6.js","site-chunks/javascript-CCiqnvv_.js","site-chunks/jsonMode-CQ_gX_ia.css","site-chunks/jsonMode-DS77d32A.js","site-chunks/markdown-Cimd5fb3.js","site-chunks/monaco-BJ215w34.css","site-chunks/monaco-DlAjVggi.js","site-chunks/monaco.contribution-D_KeWiSn.js","site-chunks/python-BbOet-pt.js","site-chunks/register-37nRgXRc.js","site-chunks/register-C23EUt-2.js","site-chunks/register-CM23zLUm.js","site-chunks/register-CRLrpJJa.js","site-chunks/register-DDVNSJs_.js","site-chunks/register-DJBIoW77.js","site-chunks/register-DQPuvLeu.js","site-chunks/register-De_SrGds.js","site-chunks/register-Dz3tCkTn.js","site-chunks/register-PKQ6UWhE.js","site-chunks/register-gUb5pvZL.js","site-chunks/register-iMY9e_D8.js","site-chunks/register-nKwNACOW.js","site-chunks/register-xuFMDy1I.js","site-chunks/rust-DdL9SqIa.js","site-chunks/shell-CC2rA5mh.js","site-chunks/site-app-CNyt3qRa.js","site-chunks/sql-NEE52Syq.js","site-chunks/tools-D4RNbUFb.js","site-chunks/typescript-DGktDBlU.js","site-chunks/web-PVFkuBVy.js","site-chunks/xml-HkNHSe_W.js","site-chunks/yaml-LzaGdrmf.js","site.css","tools/index.html","wordle.html","work/index.html"];

const relativePath = request => {
  const url = new URL(request.url);
  return url.pathname.startsWith(ROOT.pathname) ? url.pathname.slice(ROOT.pathname.length) : '';
};
const immutableAsset = request => {
  const path = relativePath(request);
  return path.startsWith('site-chunks/') || path.startsWith('assets/') || path.startsWith('keybr-assets/');
};
const cacheKey = request => {
  const url = new URL(request.url);
  url.searchParams.delete('v');
  return url.href;
};
const cacheResponse = async (request, response) => {
  if (response.ok) (await caches.open(CACHE)).put(cacheKey(request), response.clone());
  return response;
};
const staleChunkResponse = request => new Response(
  'location.reload();\nawait new Promise(() => {});\n//# sourceURL=' + request.url,
  { status: 200, headers: { 'Content-Type': 'text/javascript; charset=utf-8', 'Cache-Control': 'no-store' } },
);
const fetchFresh = async request => {
  const response = await fetch(request);
  if ((response.status === 404 || response.status === 410) && immutableAsset(request) && new URL(request.url).pathname.endsWith('.js'))
    return staleChunkResponse(request);
  return cacheResponse(request, response);
};
const networkFirst = async request => {
  try { return await fetchFresh(request); }
  catch (error) {
    const cached = await (await caches.open(CACHE)).match(cacheKey(request));
    if (cached) return cached;
    throw error;
  }
};
const cacheFirstImmutable = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  return fetchFresh(request);
};

self.addEventListener('install', event => {
  event.waitUntil(Promise.all([
    caches.open(CACHE).then(cache => cache.addAll(CORE.map(path => new URL(path, ROOT)))),
    self.skipWaiting(),
  ]));
});

self.addEventListener('activate', event => {
  event.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith(CACHE_PREFIX) && key !== CACHE).map(key => caches.delete(key)))),
    self.clients.claim(),
  ]));
});

const offlineNavigation = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  const url = new URL(request.url);
  if (url.pathname.endsWith('/')) {
    const directoryIndex = await cache.match(new URL('index.html', url));
    if (directoryIndex) return directoryIndex;
  } else if (!url.pathname.split('/').pop()?.includes('.')) {
    const htmlPage = await cache.match(new URL(url.pathname + '.html', url.origin));
    if (htmlPage) return htmlPage;
    const directoryIndex = await cache.match(new URL(url.pathname + '/index.html', url.origin));
    if (directoryIndex) return directoryIndex;
  }
  return cache.match(new URL('index.html', ROOT));
};

self.addEventListener('fetch', event => {
  if (event.request.method !== 'GET' || new URL(event.request.url).origin !== location.origin) return;
  if (event.request.mode === 'navigate') {
    event.respondWith((async () => {
      try {
        const response = await fetch(event.request);
        if (response.ok) {
          (await caches.open(CACHE)).put(event.request, response.clone());
          return response;
        }
        return (await offlineNavigation(event.request)) || response;
      } catch { return offlineNavigation(event.request); }
    })());
    return;
  }
  // Only content-hashed Vite assets are cache-first. Mutable shell/runtime,
  // workers and WASM are network-first so a deployment cannot mix generations.
  event.respondWith(immutableAsset(event.request) ? cacheFirstImmutable(event.request) : networkFirst(event.request));
});
