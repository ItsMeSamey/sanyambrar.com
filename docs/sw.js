// Generated by build.ts. Do not edit.
const CACHE_PREFIX = 'samey-site-';
const CACHE = CACHE_PREFIX + 'e11d12a7b626afe3';
const ROOT = new URL('./', self.registration.scope);
const CORE = ["assets/cnn-BlsjeOsa.wasm","assets/cnn-worker-EAlDjyUS.js","assets/editor.worker-DKXjC3Lf.js","assets/json.worker-izi4BK3w.js","blog/index.html","blog/posts/btop-mutex.html","chain/index.html","index.html","keybr.html","projects/cnn/index.html","projects/oneserial/index.html","projects/reverb/index.html","projects/zhtml/index.html","shared-runtime.js","site-chunks/Blog-D1oHRzct.js","site-chunks/Chain-Bh-GNh-J.js","site-chunks/Chain-RAwAYXH1.css","site-chunks/ChainLogo-0WtmnUs6.js","site-chunks/CnnDemo-D5Gr6BSU.js","site-chunks/Home-Dzei-KCz.js","site-chunks/Project-VDnqXvhX.js","site-chunks/ReverbDemo-CFtOIj7o.js","site-chunks/SiteChrome-CbflE6NH.js","site-chunks/Tools-4Fih52LR.css","site-chunks/Tools-BsdDqQL6.js","site-chunks/Work-Pt6yU-Kg.js","site-chunks/_.contribution-BRBHLZXc.js","site-chunks/catalog-DfLaJuYE.js","site-chunks/chain-CzzmjH8a.js","site-chunks/cpp-C7h46wYY.js","site-chunks/css-DIMkf-bt.js","site-chunks/devicePixelRatio-DKz4sG8K.js","site-chunks/dist-Bly7K4No.js","site-chunks/dist-CoN7n9_h.css","site-chunks/game-settings-CLZv-Ohu.css","site-chunks/go-C-y9NEjX.js","site-chunks/home-D0_9myQl.css","site-chunks/html-C6Nh3Km7.js","site-chunks/java-BEtHBSE6.js","site-chunks/javascript-B9gxjQGf.js","site-chunks/jsonMode-CQ_gX_ia.css","site-chunks/jsonMode-mHAg6AB4.js","site-chunks/markdown-Cimd5fb3.js","site-chunks/monaco-BJ215w34.css","site-chunks/monaco-DTrmeVso.js","site-chunks/monaco.contribution-ElnyqhrN.js","site-chunks/python-BIUnfN7o.js","site-chunks/register-BBjS3O1D.js","site-chunks/register-CNalso7c.js","site-chunks/register-ChGfz7Us.js","site-chunks/register-D4t5bV25.js","site-chunks/register-DeIKRa8B.js","site-chunks/register-VOsjHNWW.js","site-chunks/register-_Wr5HeXT.js","site-chunks/register-gKqfyclX.js","site-chunks/register-hWYwwya4.js","site-chunks/register-iHo8Z74l.js","site-chunks/register-kDKzqsp3.js","site-chunks/register-lsEYt5Jr.js","site-chunks/register-sTOZGwak.js","site-chunks/register-zj_iFOSw.js","site-chunks/rust-DdL9SqIa.js","site-chunks/shell-CC2rA5mh.js","site-chunks/site-app-DjtooASZ.js","site-chunks/sql-NEE52Syq.js","site-chunks/tools-CZKRKNUV.js","site-chunks/typescript-BW8B-czU.js","site-chunks/web-PVFkuBVy.js","site-chunks/xml-CVaxilbl.js","site-chunks/yaml-kgpMz1PE.js","site.css","tools/index.html","wordle.html","work/index.html"];

const relativePath = request => {
  const url = new URL(request.url);
  return url.pathname.startsWith(ROOT.pathname) ? url.pathname.slice(ROOT.pathname.length) : '';
};
const immutableAsset = request => {
  const path = relativePath(request);
  return path.startsWith('site-chunks/') || path.startsWith('assets/') || path.startsWith('keybr-assets/');
};
const cacheKey = request => {
  const url = new URL(request.url);
  url.searchParams.delete('v');
  return url.href;
};
const cacheResponse = async (request, response) => {
  if (response.ok) (await caches.open(CACHE)).put(cacheKey(request), response.clone());
  return response;
};
const staleChunkResponse = request => new Response(
  'location.reload();\nawait new Promise(() => {});\n//# sourceURL=' + request.url,
  { status: 200, headers: { 'Content-Type': 'text/javascript; charset=utf-8', 'Cache-Control': 'no-store' } },
);
const fetchFresh = async request => {
  const response = await fetch(request);
  if ((response.status === 404 || response.status === 410) && immutableAsset(request) && new URL(request.url).pathname.endsWith('.js'))
    return staleChunkResponse(request);
  return cacheResponse(request, response);
};
const networkFirst = async request => {
  try { return await fetchFresh(request); }
  catch (error) {
    const cached = await (await caches.open(CACHE)).match(cacheKey(request));
    if (cached) return cached;
    throw error;
  }
};
const cacheFirstImmutable = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  return fetchFresh(request);
};

self.addEventListener('install', event => {
  event.waitUntil(Promise.all([
    caches.open(CACHE).then(cache => cache.addAll(CORE.map(path => new URL(path, ROOT)))),
    self.skipWaiting(),
  ]));
});

self.addEventListener('activate', event => {
  event.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith(CACHE_PREFIX) && key !== CACHE).map(key => caches.delete(key)))),
    self.clients.claim(),
  ]));
});

const offlineNavigation = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  const url = new URL(request.url);
  if (url.pathname.endsWith('/')) {
    const directoryIndex = await cache.match(new URL('index.html', url));
    if (directoryIndex) return directoryIndex;
  } else if (!url.pathname.split('/').pop()?.includes('.')) {
    const htmlPage = await cache.match(new URL(url.pathname + '.html', url.origin));
    if (htmlPage) return htmlPage;
    const directoryIndex = await cache.match(new URL(url.pathname + '/index.html', url.origin));
    if (directoryIndex) return directoryIndex;
  }
  return cache.match(new URL('index.html', ROOT));
};

self.addEventListener('fetch', event => {
  if (event.request.method !== 'GET' || new URL(event.request.url).origin !== location.origin) return;
  if (event.request.mode === 'navigate') {
    event.respondWith((async () => {
      try {
        const response = await fetch(event.request);
        if (response.ok) {
          (await caches.open(CACHE)).put(event.request, response.clone());
          return response;
        }
        return (await offlineNavigation(event.request)) || response;
      } catch { return offlineNavigation(event.request); }
    })());
    return;
  }
  // Only content-hashed Vite assets are cache-first. Mutable shell/runtime,
  // workers and WASM are network-first so a deployment cannot mix generations.
  event.respondWith(immutableAsset(event.request) ? cacheFirstImmutable(event.request) : networkFirst(event.request));
});
