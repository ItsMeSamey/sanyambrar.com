// Generated by build.ts. Do not edit.
const CACHE_PREFIX = 'samey-site-';
const CACHE = CACHE_PREFIX + 'ae48a5c5ea2dba77';
const ROOT = new URL('./', self.registration.scope);
const CORE = ["assets/cnn-BlsjeOsa.wasm","assets/cnn-worker-EAlDjyUS.js","assets/editor.worker-DKXjC3Lf.js","assets/json.worker-izi4BK3w.js","blog/index.html","blog/posts/btop-mutex.html","chain/index.html","index.html","keybr.html","projects/cnn/index.html","projects/oneserial/index.html","projects/reverb/index.html","projects/zhtml/index.html","shared-runtime.js","site-chunks/Blog-BHziBCKx.js","site-chunks/Chain-BKADW5_1.js","site-chunks/Chain-RAwAYXH1.css","site-chunks/ChainLogo-CgqzlrLt.js","site-chunks/CnnDemo-D9jrkC3x.js","site-chunks/Home-a6V4EhKN.js","site-chunks/Project-t6E-r7NR.js","site-chunks/ReverbDemo-Bi9Ibq4S.js","site-chunks/SiteChrome-YWEc0VhW.js","site-chunks/Tools--7GcVNfZ.js","site-chunks/Tools-C4n0MwU8.css","site-chunks/Work-ClwzqSKB.js","site-chunks/_.contribution-CLL8JMws.js","site-chunks/catalog-DfLaJuYE.js","site-chunks/chain-fKHq-J-m.js","site-chunks/cpp-C7h46wYY.js","site-chunks/css-DIMkf-bt.js","site-chunks/dist-CoN7n9_h.css","site-chunks/dist-W9r3Kmvf.js","site-chunks/game-settings-0Zxoa_jh.css","site-chunks/go-C-y9NEjX.js","site-chunks/home-D0_9myQl.css","site-chunks/html-DS5WNAL4.js","site-chunks/java-BEtHBSE6.js","site-chunks/javascript-BcEoC9Lw.js","site-chunks/jsonMode-CQ_gX_ia.css","site-chunks/jsonMode-DYKOQvlB.js","site-chunks/markdown-Cimd5fb3.js","site-chunks/monaco-BJ215w34.css","site-chunks/monaco-DoSlu89N.js","site-chunks/monaco.contribution-BFHcz-9U.js","site-chunks/python-DhkB7MT5.js","site-chunks/register-9_iAG1fq.js","site-chunks/register-B4hlJ2e3.js","site-chunks/register-BBATzCev.js","site-chunks/register-BWmbMG3x.js","site-chunks/register-Bg_G5bWM.js","site-chunks/register-CSIFs6RI.js","site-chunks/register-CyPkpZ-4.js","site-chunks/register-DJkv6j7x.js","site-chunks/register-DMdHWMEs.js","site-chunks/register-DsJ5jwi8.js","site-chunks/register-Gb_ha_dz.js","site-chunks/register-JZfbLicX.js","site-chunks/register-MOmWm5Py.js","site-chunks/register-QTQbS2_k.js","site-chunks/rust-DdL9SqIa.js","site-chunks/shell-CC2rA5mh.js","site-chunks/site-app-qmh3853P.js","site-chunks/sql-NEE52Syq.js","site-chunks/tools-BonsjsHU.js","site-chunks/typescript-B3ijM_Fh.js","site-chunks/web-PVFkuBVy.js","site-chunks/xml-BESC05Xi.js","site-chunks/yaml-jSxcWKe6.js","site.css","tools/index.html","wordle.html","work/index.html"];

const relativePath = request => {
  const url = new URL(request.url);
  return url.pathname.startsWith(ROOT.pathname) ? url.pathname.slice(ROOT.pathname.length) : '';
};
const immutableAsset = request => {
  const path = relativePath(request);
  return path.startsWith('site-chunks/') || path.startsWith('assets/') || path.startsWith('keybr-assets/');
};
const cacheKey = request => {
  const url = new URL(request.url);
  url.searchParams.delete('v');
  return url.href;
};
const cacheResponse = async (request, response) => {
  if (response.ok) (await caches.open(CACHE)).put(cacheKey(request), response.clone());
  return response;
};
const staleChunkResponse = request => new Response(
  'location.reload();\nawait new Promise(() => {});\n//# sourceURL=' + request.url,
  { status: 200, headers: { 'Content-Type': 'text/javascript; charset=utf-8', 'Cache-Control': 'no-store' } },
);
const fetchFresh = async request => {
  const response = await fetch(request);
  if ((response.status === 404 || response.status === 410) && immutableAsset(request) && new URL(request.url).pathname.endsWith('.js'))
    return staleChunkResponse(request);
  return cacheResponse(request, response);
};
const networkFirst = async request => {
  try { return await fetchFresh(request); }
  catch (error) {
    const cached = await (await caches.open(CACHE)).match(cacheKey(request));
    if (cached) return cached;
    throw error;
  }
};
const cacheFirstImmutable = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  return fetchFresh(request);
};

self.addEventListener('install', event => {
  event.waitUntil(Promise.all([
    caches.open(CACHE).then(cache => cache.addAll(CORE.map(path => new URL(path, ROOT)))),
    self.skipWaiting(),
  ]));
});

self.addEventListener('activate', event => {
  event.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(key => key.startsWith(CACHE_PREFIX) && key !== CACHE).map(key => caches.delete(key)))),
    self.clients.claim(),
  ]));
});

const offlineNavigation = async request => {
  const cache = await caches.open(CACHE);
  const cached = await cache.match(request);
  if (cached) return cached;
  const url = new URL(request.url);
  if (url.pathname.endsWith('/')) {
    const directoryIndex = await cache.match(new URL('index.html', url));
    if (directoryIndex) return directoryIndex;
  } else if (!url.pathname.split('/').pop()?.includes('.')) {
    const htmlPage = await cache.match(new URL(url.pathname + '.html', url.origin));
    if (htmlPage) return htmlPage;
    const directoryIndex = await cache.match(new URL(url.pathname + '/index.html', url.origin));
    if (directoryIndex) return directoryIndex;
  }
  return cache.match(new URL('index.html', ROOT));
};

self.addEventListener('fetch', event => {
  if (event.request.method !== 'GET' || new URL(event.request.url).origin !== location.origin) return;
  if (event.request.mode === 'navigate') {
    event.respondWith((async () => {
      try {
        const response = await fetch(event.request);
        if (response.ok) {
          (await caches.open(CACHE)).put(event.request, response.clone());
          return response;
        }
        return (await offlineNavigation(event.request)) || response;
      } catch { return offlineNavigation(event.request); }
    })());
    return;
  }
  // Only content-hashed Vite assets are cache-first. Mutable shell/runtime,
  // workers and WASM are network-first so a deployment cannot mix generations.
  event.respondWith(immutableAsset(event.request) ? cacheFirstImmutable(event.request) : networkFirst(event.request));
});
